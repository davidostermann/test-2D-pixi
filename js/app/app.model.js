define( [
    
  ], 
  function() {
    'use strict';

    var _singleton = {
      stage: null,
      renderer: null,
      fullWidth: null,
      fullHeight: null,
    };
    return _singleton;
});
